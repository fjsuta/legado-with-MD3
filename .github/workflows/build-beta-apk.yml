name: Build Beta APK

on:
  workflow_dispatch:
    inputs:
      branch:
        description: '要构建的分支'
        required: true
        default: 'trae/solo-agent-vhkVM3'
      release_tag:
        description: 'Release 标签 (留空则不上传到 Release)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.inputs.branch }}
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Clear 18PlusList.txt
        run: |
          echo "清空18PlusList.txt"
          echo "" > $GITHUB_WORKSPACE/app/src/main/assets/18PlusList.txt

      - name: Build Beta APK
        run: |
          chmod +x gradlew
          ./gradlew assembleAppBeta --no-daemon
        env:
          COMMIT_NUMBER: ${{ github.run_number }}
          APP_VERSION_NAME: 3.26.0-Beta${{ github.run_number }}

      - name: Check Build Output
        run: |
          APK_PATH="$GITHUB_WORKSPACE/app/build/outputs/apk/app/beta"
          echo "检查 APK 输出目录: $APK_PATH"
          ls -la "$APK_PATH" || true
          if [ -z "$(find "$APK_PATH" -name "*.apk" -print -quit)" ]; then
            echo "未找到 APK 文件！"
            exit 1
          fi

      - name: Rename APK Files
        run: |
          APK_PATH="$GITHUB_WORKSPACE/app/build/outputs/apk/app/beta"
          cd "$APK_PATH"
          for f in *.apk; do
            mv "$f" "legado-Beta-${GITHUB_RUN_NUMBER}-${f}"
          done
          ls -la

      - name: Create Source Code ZIP Archive (excluding md files)
        run: |
          cd "$GITHUB_WORKSPACE"
          ZIP_NAME="legado-source-${GITHUB_RUN_NUMBER}.zip"
          zip -r "$GITHUB_WORKSPACE/$ZIP_NAME" . \
            -x "*.git/*" \
            -x "*.gradle/*" \
            -x "*.idea/*" \
            -x "build/*" \
            -x "*/build/*" \
            -x "*.apk" \
            -x "*.zip" \
            -x "**/*.md" \
            -x "**/*.MD" \
            -x "LICENSE*" \
            -x "README*"
          echo "源码压缩包已创建: $ZIP_NAME"
          ls -la "$GITHUB_WORKSPACE/$ZIP_NAME"

      - name: Create APK ZIP Archive
        run: |
          APK_PATH="$GITHUB_WORKSPACE/app/build/outputs/apk/app/beta"
          ZIP_NAME="legado-Beta-apks-${GITHUB_RUN_NUMBER}.zip"
          cd "$APK_PATH"
          zip "$GITHUB_WORKSPACE/$ZIP_NAME" *.apk
          echo "APK 压缩包已创建: $ZIP_NAME"
          ls -la "$GITHUB_WORKSPACE/$ZIP_NAME"

      - name: Upload APK Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: legado-Beta-apks-${{ github.run_number }}
          path: app/build/outputs/apk/app/beta/*.apk

      - name: Upload Source Code ZIP Artifact
        uses: actions/upload-artifact@v4
        with:
          name: legado-source-zip-${{ github.run_number }}
          path: legado-source-*.zip

      - name: Upload APK ZIP Artifact
        uses: actions/upload-artifact@v4
        with:
          name: legado-Beta-apks-zip-${{ github.run_number }}
          path: legado-Beta-apks-*.zip

      - name: Create Release and Upload
        if: ${{ github.event.inputs.release_tag != '' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.release_tag }}
          name: Legado Beta Build ${{ github.event.inputs.release_tag }}
          body: |
            自动构建的 Beta 版本
            
            - 分支: ${{ github.event.inputs.branch }}
            - 构建号: ${{ github.run_number }}
            - 版本: 3.26.0-Beta${{ github.run_number }}
            
            **包含文件：**
            - APK 安装包 (Beta)
            - 源码压缩包（已排除 .md 文件）
          prerelease: true
          files: |
            app/build/outputs/apk/app/beta/*.apk
            legado-Beta-apks-*.zip
            legado-source-*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
